version: '3.8'
services:
  audiogen:
    build: .
    container_name: audiogen_gpu
    volumes:
      - .:/app
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: 1
              capabilities: [gpu]
